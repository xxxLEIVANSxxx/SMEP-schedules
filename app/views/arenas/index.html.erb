<h1 class="header">Arenas</h1>

<table class="highlight">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @arenas.each do |arena| %>
      <tr>
        <td><%= arena.name %></td>
        <td><%= arena.address %></td>
        <td><%= link_to 'Show', arena, :class => "btn-flat" %></td>
        <td><%= link_to 'Edit', edit_arena_path(arena), :class => "btn-flat" %></td>
        <td><%= link_to 'Destroy', arena, method: :delete, data: { alert: 'Are you sure?' }, :class => "btn-flat" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Arena', new_arena_path, :class => "btn-flat" %>
